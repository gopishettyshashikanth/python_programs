To install pip:
sudo apt-get install python-pip

sudo apt-get install git
sudo apt-get install vim


Command /usr/bin/python -c "import setuptools, tokenize;__file__='/tmp/pip_build_user/tornado/setup.py';exec(compile(getattr(tokenize, 'open', open)(__file__).read().replace('\r\n', '\n'), __file__, 'exec'))" install --record /tmp/pip-xJ9WtR-record/install-record.txt --single-version-externally-managed --compile failed with error code 1 in /tmp/pip_build_user/tornado
Storing debug log for failure in /home/user/.pip/pip.log

if you get the above error need to execute the below command

sudo pip install --upgrade pip

if you want install dependences, write all dependences in one text file and save it(any name) requirements.txt

sudo pip install -r pip-requirements.txt

or 

pip install -U -r pip-requires.txt


pip freeze------------>check installed packages

supervisord------>run server
supervisorctl---->check run

eg:http://localhost:8004/call?id=123&peer_id=121------------------>caller


eg:http://localhost:8004/recieve?id=121&peer_id=123---------------->receive

webrtc path:
sudo vim /etc/nginx/nginx.conf


python 
sudo apt-get install python-dev

know the ubuntu version
lsb_release -a



vidmate------------>E: Unable to correct problems, you have held broken packages.
sudo aptitude install myNewPackage
If you don't have aptitude on your machine yet, get it with

sudo apt-get install aptitude


nano edit-------------->ctrl:w + enter

postgres editor(connect to database:)
-----------
sudo su - postgres
psql
\l : list od db's
connect db : \c database name
database tables : \dt  
\d table name : describe table 


ALTER USER postgres with password ‘new-password’;

comeout from db
----------------
\q
exit
list of database's : \l
connect to particular db : \c database name
\dt :-  database tables


environment creation
---------------------
1. sudo pip install virtualenv
2. sudo pip install virtualenvwrapper
3. vim ~/.bash_profile
export WORKON_HOME=$HOME/.virtualenvs
source /usr/local/bin/virtualenvwrapper.sh

4. source ~/.bash_profile

5. mkvirtualenv my_env or virtualenv my_env
6. workon my_env

got to environment : 
------------------
source vidmedenv/bin/activate

comeout from environment 
--------------------

deactivate

Django
------

django-admin startproject myproject

python manage.py startapp myapp

pip install Django==1.8.1

connect to mysql
--------------
mysql -u root -p 

mysql upgrade stepe
-------------------
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install mysql-server-5.6

connect to another system
-------------------------
ex:--
sudo scp -r administrator@202.153.34.163:/etc/init.d/red5 .

sharing my files some other system
---------------------------------
python -m SimpleHTTPServer 8005


nginx
-------
sudo service nginx start
sudo service nginx stop
sudo service nginx restart

or  

sudo systemctl start nginx 
sudo systemctl stop nginx 
sudo systemctl restart nginx


local db export
-------------------
mysqldump -uroot -p123456aA anmol_django > db.sql

local db import
-------------------- 
mysqldump -uroot -p123456aA anmol_django < db.sql

truncate table accounts_servicescountstransactions;


sudo service network-manager restart

ping google.com

checking port number's running
---------------------------------
ps aux | grep 8000

sudo kill portnumber(8000)


python manage.py runserver 8010



#user_obj = from.save(commit=False)
#user_obj.pf = (form.c)


install requirements.txt
pip install -r pip-requirements.txt


postges import queries:

psql -u username -d databsename -f filename

postgres@DIPL-CP-PC008:~$ psql -U postgres -d cms_aug_29  -f /home/user/Downloads/dump.sql

permissions to the file:
-----------------------
need to go that folder and give permission 
user@DIPL-CP-PC008:~/Downloads$ sudo chown postgres:postgres dump.sql


text = """<h1>welcome to my app !</h1>"""
   		return HttpResponse(text)

multiple image upload:
http://www.easydevmixin.com/2015/07/17/easily-dealing-with-multiple-files-in-django/


https://docs.djangoproject.com/en/1.11/topics/http/file-uploads/

excel upload
http://thepythondjango.com/upload-process-csv-file-django/

https://simpleisbetterthancomplex.com/tutorial/2016/08/01/how-to-upload-files-with-django.html
